# Copie para .env.compose e ajuste a senha (use %21 no lugar de ! na URL).
# Usado quando o backend roda via Podman Compose (rede interna: host "mysql").

DATABASE_URL="mysql://acasa_user:SENHA_URL_ENCODED@mysql:3306/acasa_erp"
