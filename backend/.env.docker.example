# Copie para .env.docker e ajuste a senha (use %21 no lugar de ! na URL).
# Este arquivo sobrescreve apenas a conexão com o banco quando o backend roda no Docker.

DATABASE_URL="mysql://acasa_user:SENHA_URL_ENCODED@mysql:3306/acasa_erp"
