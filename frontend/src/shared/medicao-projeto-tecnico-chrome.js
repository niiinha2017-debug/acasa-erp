import { ref } from 'vue'

/** Modo Projeto Técnico: sem menu ERP + layout split 2D/3D (medição fina). */
export const medicaoProjetoTecnicoImmersivo = ref(false)

/** 'split' | '2d' | '3d' — em tela cheia, alternância rápida entre vistas */
export const medicaoProjetoTecnicoPainel = ref('split')
